# 角色系统英文国际化文件
# Role System English Internationalization File
# File: src/main/resources/i18n/en/en-role.yml

role:
  # Extractor
  extractor:
    initialized: "Role knowledge extractor initialized"
    features_loaded: "Role features loaded: {} roles"
    extracting: "Extracting role knowledge: userId={}, data size={}"
    extracted: "Role knowledge extraction completed: {} items"
    extract_failed: "Role knowledge extraction failed: {}"
    batch_extracting: "Batch extracting: {} users"
    batch_extracted: "Batch extraction completed: {} role knowledge items"

  # Aggregator
  aggregator:
    initialized: "Role knowledge aggregator initialized"
    aggregating: "Aggregating role knowledge: {} items"
    aggregated: "Aggregation completed: {} → {} items"
    aggregate_failed: "Aggregation failed: {}"
    aggregating_by_role: "Aggregating by role: {} roles"
    role_aggregated: "Role aggregation completed: roleId={}, {} → {} items"

  # Gap Analyzer
  gap:
    initialized: "Knowledge gap analyzer initialized"
    domains_loaded: "Expected domains loaded: {} roles"
    analyzing: "Analyzing knowledge gaps: roleId={}, knowledge count={}"
    analyzed: "Gap analysis completed: roleId={}, covered {}/{}"
    analyze_failed: "Gap analysis failed: {}"
    batch_analyzing: "Batch analyzing: {} roles"
  config:
    disabled: "🔕 Role system is disabled"
    loaded: "✅ Role configuration loaded, {0} roles in total"
    file:
      notfound: "⚠️ Role config file not found: {0}, loading default config"
    format:
      invalid: "⚠️ Invalid role config file format, loading default config"
    load:
      error: "❌ Failed to load role config: {0}"
    loading:
      path: "📂 Loading role configuration: {0}"
      default: "📂 Loading default role configuration"
    default:
      loaded: "✅ Default role configuration loaded, {0} roles in total"
    role:
      loaded: "✅ Role loaded successfully: id={0}, name={1}"
      parse:
        error: "❌ Failed to parse role config: id={0}, error={1}"
  
  # Role Query (RoleController)
  query:
    # Parameters: {0}=keyword, {1}=page, {2}=pageSize
    start: "🔍 Querying roles: keyword={0}, page={1}, pageSize={2}"
    # Parameters: {0}=total, {1}=returned
    success: "✅ Query roles successful: total={0}, returned={1}"
    failed: "❌ Query roles failed"
  
  # Role List Loading
  list:
    # Parameters: {0}=total, {1}=returned
    loaded: "✅ Role list loaded successfully: total={0}, returned={1}"
    empty: "📋 Role list is empty"
  
  # Role Creation
  create:
    # Parameters: {0}=roleName, {1}=roleId
    success: "✅ Role created successfully: {0} ({1})"
    failed: "❌ Failed to create role"
    # Mock mode
    mock: "🔧 Mock mode: Creating role {0}"
  
  # Role Update
  update:
    # Parameters: {0}=roleName, {1}=roleId
    success: "✅ Role updated successfully: {0} ({1})"
    failed: "❌ Failed to update role"
    notfound: "⚠️ Role not found: {0}"
    # Mock mode
    mock: "🔧 Mock mode: Updating role {0}"
  
  # Role Deletion
  delete:
    # Parameters: {0}=roleId
    success: "✅ Role deleted successfully: {0}"
    failed: "❌ Failed to delete role"
    # Mock mode
    mock: "🔧 Mock mode: Deleting role {0}"
  
  # Role Detection
  detect:
    # Parameters: {0}=question
    start: "🚀 Starting role detection: question={0}"
    # Parameters: {0}=roleName, {1}=confidence
    success: "✅ Role detection successful: role={0}, confidence={1}"
    failed: "❌ Role detection failed"
    mock: "🔧 Mock mode: Role detector unavailable"
    empty-question: "⚠️ Question cannot be empty"
  
  # Filter Operations
  filter:
    # Parameters: {0}=keyword
    applying: "🔍 Applying keyword filter: {0}"
  
  # Sort Operations
  sort:
    # Parameters: {0}=sortBy, {1}=sortOrder
    applying: "📊 Applying sort: sortBy={0}, order={1}"

  manager:
    initialized: "✅ Role manager initialized"
    role:
      notfound: "⚠️ Role not found: {0}, using default role"
    usage:
      recorded: "📊 Role usage recorded: {0}, total usage {1} times"
    reloading: "🔄 Reloading role configuration..."
    reloaded: "✅ Role configuration reloaded, {0} roles in total"

