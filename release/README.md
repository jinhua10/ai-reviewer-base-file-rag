# 知识库问答系统 - 部署包

**最新版本特性：**
- ✅ **OCR图片文字识别** - 自动提取文档中图片的文字内容
- ✅ **PDFBox修复** - 修复PDF文件解析兼容性问题
- ✅ **增强日志** - 清晰显示OCR处理状态
- ✅ **诊断工具** - 一键检查OCR配置（`check-ocr.bat`）

models下提供bat下载模型的脚本

## 📦 目录结构

```
release/
├── ai-reviewer-base-file-rag-1.0.jar  # 应用JAR包
├── start.bat                           # 启动脚本（Windows）
├── stop.bat                            # 停止脚本（Windows）
├── fix-lock.bat                        # 修复索引锁脚本（Windows）
├── download-tessdata.bat               # OCR语言包下载脚本
├── README.md                           # 本文件
├── 模型下载说明.md                     # 模型下载指南
├── 快速启动-OCR.md                     # 🆕 OCR快速启动指南（推荐先看）
├── 图片识别快速启用.md                 # 🆕 OCR完整使用指南
├── OCR配置指南.md                      # OCR详细配置文档
├── OCR诊断指南.md                      # 🆕 OCR问题排查指南
├── check-ocr.bat                       # 🆕 OCR配置检查脚本
├── config/
│   └── application.yml                 # 外置配置文件
├── models/                             # 模型目录（外部）
│   └── paraphrase-multilingual/
│       └── model.onnx                  # 向量模型文件
├── tessdata/                           # 🆕 OCR语言包目录（运行download-tessdata.bat后生成）
│   ├── chi_sim.traineddata            # 中文简体
│   └── eng.traineddata                # 英文
├── data/
│   ├── documents/                      # 文档目录（放置要索引的文档）
│   ├── knowledge-base/                 # 知识库存储（自动生成）
│   └── rag/                            # RAG数据（自动生成）
└── logs/
    └── knowledge-qa-system.log         # 应用日志（自动生成）
```

---

## 🚀 快速开始

### 1. 环境要求

- **JDK**: 17 或更高版本
- **内存**: 建议 2GB 以上
- **磁盘**: 根据文档量，建议预留 5GB 以上

### 2. 准备文档

将要索引的文档放到 `data/documents/` 目录：

```
data/documents/
├── 产品手册.pdf
├── 技术文档.docx
├── 数据统计.xlsx
└── ...
```

**支持的格式**:
- Excel: `.xlsx`, `.xls`
- Word: `.docx`, `.doc`
- PowerPoint: `.pptx`, `.ppt` 🆕 **支持提取图片中的文字**
- PDF: `.pdf`
- 文本: `.txt`, `.md`
- 其他: `.html`, `.xml`

> 💡 **新功能**：支持从PPTX/DOCX/XLSX中的**图片提取文字**！
> 
> 如果您的文档中只有图片，没有文字，可以启用OCR功能自动识别图片中的内容。
> 
> 📖 快速启用指南：[图片识别快速启用.md](图片识别快速启用.md)

### 3. （可选）启用OCR图片识别

如果您的文档包含大量图片（如PPT、扫描件），建议启用OCR：

```batch
# 下载OCR语言包（只需一次）
download-tessdata.bat
```

> ✅ OCR已默认启用！下载语言包后无需任何配置。
> 
> 📖 详细说明：[快速启动-OCR.md](快速启动-OCR.md)
> 
> 🔧 遇到问题？运行 `check-ocr.bat` 诊断

### 4. 启动应用

⚠️ **重要提示**: 
- 默认使用**关键词检索**模式，无需下载模型，开箱即用
- 如需启用**向量检索**（语义搜索），请参考 [模型下载说明.md](模型下载说明.md)

**方式1: 双击启动**
- 双击 `start.bat` 文件

**方式2: 命令行启动**
```batch
start.bat
```

### 4. 验证运行

启动成功后，访问以下地址验证：

```bash
# 健康检查
curl http://localhost:8080/api/qa/health

# 获取统计信息
curl http://localhost:8080/api/qa/statistics

# 搜索测试
curl "http://localhost:8080/api/qa/search?query=关键词&limit=5"
```

### 5. 停止应用

**方式1: 双击停止**
- 双击 `stop.bat` 文件

**方式2: 命令行停止**
```batch
stop.bat
```

**方式3: Ctrl+C**
- 在启动窗口按 `Ctrl+C`

---

## ⚙️ 配置说明

### 修改配置

编辑 `config/application.yml` 文件：

```yaml
knowledge:
  qa:
    knowledge-base:
      # 文档源路径
      source-path: ./data/documents
      
      # 是否重建知识库
      rebuild-on-startup: false
      
    llm:
      # LLM提供商: mock, openai, deepseek
      provider: mock
      
      # API Key（如使用真实LLM）
      api-key: ${AI_API_KEY:}
```

### 常用配置项

#### 1. 修改端口

```yaml
server:
  port: 8080  # 改为其他端口，如 9090
```

#### 2. 启用向量检索

⚠️ **注意**: 启用前需要先下载模型文件，详见 [模型下载说明.md](模型下载说明.md)

```yaml
knowledge:
  qa:
    vector-search:
      # false: 关键词检索（默认，无需模型）
      # true:  向量检索（需要下载模型）
      enabled: false
```

**两种模式对比**:

| 模式 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| 关键词检索 | 快速、零配置 | 仅精确匹配 | 精确查询 |
| 向量检索 | 语义理解、模糊匹配 | 需要模型、占内存 | 模糊查询 |

#### 3. 配置真实LLM

```yaml
knowledge:
  qa:
    llm:
      provider: openai
      api-key: sk-your-api-key  # 或使用环境变量
      model: gpt-4o
```

**设置环境变量**（推荐）:
```batch
set AI_API_KEY=sk-your-api-key
```

#### 4. 调整内存限制

编辑 `start.bat`，修改 `-Xmx` 参数：

```batch
set JAVA_OPTS=%JAVA_OPTS% -Xms512m -Xmx4g  :: 改为 4GB
```

---

## 📊 首次启动流程

### 1. 启动应用

```batch
start.bat
```

### 2. 观察日志

应该看到类似输出：

```
================================================================================
📚 知识库问答系统初始化中...
================================================================================

🔨 步骤1: 初始化知识库
   - 存储路径: ./data/knowledge-base
   - 文档路径: ./data/documents
   - 重建模式: 否（使用已有知识库）

📂 扫描文档: ./data/documents
✓ 找到 150 个文档文件

📝 开始处理文档...
📄 处理: 产品手册.pdf (2.5 MB)
   ✓ 提取 15,234 字符
   ✓ 已索引

... (处理所有文档)

================================================================================
✅ 知识库构建完成
================================================================================
   - 成功: 150 个文件
   - 失败: 0 个文件
   - 总文档: 150 个
   - 耗时: 32.45 秒
================================================================================

✅ 知识库问答系统初始化完成！
```

### 3. 测试API

```bash
# Windows PowerShell
Invoke-WebRequest -Uri "http://localhost:8080/api/qa/health"

# 或使用curl
curl http://localhost:8080/api/qa/health
```

---

## 🔍 API使用

### 1. 健康检查

```bash
GET http://localhost:8080/api/qa/health
```

**响应**:
```json
{
  "status": "UP",
  "message": "Knowledge QA System is running"
}
```

### 2. 获取统计信息

```bash
GET http://localhost:8080/api/qa/statistics
```

**响应**:
```json
{
  "documentCount": 150,
  "totalSize": "125 MB",
  "indexPath": "./data/knowledge-base",
  "lastUpdated": "2025-11-23T11:30:00"
}
```

### 3. 搜索文档

```bash
GET http://localhost:8080/api/qa/search?query=产品特性&limit=5
```

**响应**:
```json
{
  "query": "产品特性",
  "documents": [
    {
      "id": "doc-001",
      "title": "产品手册.pdf",
      "content": "...产品特性介绍...",
      "score": 0.92
    }
  ],
  "totalCount": 5
}
```

### 4. 智能问答

```bash
POST http://localhost:8080/api/qa/ask
Content-Type: application/json

{
  "question": "产品有哪些主要特性？"
}
```

**响应**:
```json
{
  "question": "产品有哪些主要特性？",
  "answer": "根据文档显示，产品主要有以下特性：...",
  "sources": [
    {
      "file": "产品手册.pdf",
      "content": "..."
    }
  ],
  "confidence": 0.89
}
```

---

## 🔧 常见问题

### 问题1: 端口被占用

**错误信息**:
```
Port 8080 is already in use
```

**解决方法**:

方法1: 修改端口
```yaml
# config/application.yml
server:
  port: 9090  # 改为其他端口
```

方法2: 停止占用端口的程序
```batch
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F
```

---

### 问题2: 找不到文档

**日志显示**:
```
未找到支持的文档文件
```

**解决方法**:
1. 确认文档已放到 `data/documents/` 目录
2. 确认文件格式在支持列表中
3. 检查配置文件中的 `source-path` 设置

---

### 问题3: 内存不足

**错误信息**:
```
Java heap space
OutOfMemoryError
```

**解决方法**:

编辑 `start.bat`，增加内存：
```batch
set JAVA_OPTS=%JAVA_OPTS% -Xms512m -Xmx4g
```

---

### 问题4: 中文乱码

**解决方法**:

1. 确认配置文件使用 UTF-8 编码保存
2. 检查 `start.bat` 中的编码设置：
   ```batch
   chcp 65001
   ```

---

### 问题5: 向量检索失败

**日志显示**:
```
向量检索引擎初始化失败
```

**解决方法**:

方法1: 禁用向量检索
```yaml
# config/application.yml
knowledge:
  qa:
    vector-search:
      enabled: false
```

方法2: 下载模型文件
- 参考 [模型下载说明.md](模型下载说明.md)
- 将模型文件放到 `models/` 目录

---

### 问题6: Lucene 索引锁定错误

**错误信息**:
```
LockObtainFailedException: Lock held by this virtual machine
```

**原因**: 上一个应用实例未正常关闭，留下了锁文件。

**解决方法**:

方法1: 删除锁文件（推荐）
```batch
# 停止应用
stop.bat

# 删除锁文件
del data\knowledge-base\index\lucene-index\write.lock

# 重新启动
start.bat
```

方法2: 清理整个索引（会重建知识库）
```batch
# 停止应用
stop.bat

# 删除索引目录
rmdir /s /q data\knowledge-base\index

# 重新启动（会自动重建索引）
start.bat
```

方法3: 确保只运行一个实例
- 检查是否有其他实例在运行
- 使用 `stop.bat` 确保完全停止

**预防措施**:
- 始终使用 `stop.bat` 正常停止应用
- 不要使用任务管理器强制结束进程
- 如果必须强制结束，记得清理锁文件

---

## 📁 数据管理

### 重建知识库

如果文档有更新，需要重建知识库：

**方法1: 修改配置**
```yaml
# config/application.yml
knowledge:
  qa:
    knowledge-base:
      rebuild-on-startup: true
```

然后重启应用。

**方法2: 删除旧索引**
```batch
# 停止应用
stop.bat

# 删除旧数据
rmdir /s /q data\knowledge-base
rmdir /s /q data\vector-index

# 重新启动
start.bat
```

### 备份数据

定期备份以下目录：
- `data/knowledge-base/` - 知识库索引
- `data/vector-index/` - 向量索引（如果启用）
- `config/application.yml` - 配置文件

### 清理日志

```batch
# 删除旧日志
del logs\*.log

# 或只保留最近7天
forfiles /p logs /s /m *.log /d -7 /c "cmd /c del @path"
```

---

## 🎯 性能优化

### 1. JVM参数优化

编辑 `start.bat`:

```batch
:: 生产环境推荐配置
set JAVA_OPTS=-Xms1g -Xmx4g
set JAVA_OPTS=%JAVA_OPTS% -XX:+UseG1GC
set JAVA_OPTS=%JAVA_OPTS% -XX:MaxGCPauseMillis=200
set JAVA_OPTS=%JAVA_OPTS% -XX:+HeapDumpOnOutOfMemoryError
set JAVA_OPTS=%JAVA_OPTS% -XX:HeapDumpPath=./logs/heapdump.hprof
```

### 2. 索引优化

```yaml
# config/application.yml
knowledge:
  qa:
    knowledge-base:
      enable-cache: true  # 启用缓存
      
    document:
      chunk-size: 2000     # 分块大小
      chunk-overlap: 400   # 重叠大小
```

### 3. 向量检索优化

```yaml
knowledge:
  qa:
    vector-search:
      top-k: 20                    # 检索数量
      similarity-threshold: 0.4    # 相似度阈值
```

---

## 📞 技术支持

- **问题反馈**: [GitHub Issues]
- **文档**: 查看 `docs/` 目录
- **日志**: 查看 `logs/knowledge-qa-system.log`

---

## 📝 更新日志

### v1.0.0 (2025-11-23)
- ✅ 首次发布
- ✅ 支持多种文档格式
- ✅ 向量检索支持
- ✅ REST API
- ✅ 外置配置

---

<div align="center">

**知识库问答系统 v1.0.0**

Made with ❤️ by AI Reviewer Team

</div>

