# RAG 2.0 代码规范与文档管理规范
# RAG 2.0 Code Standards and Document Management

> **文档编号**: 20251209-23-00-00-CODE_STANDARDS  
> **创建日期**: 2025-12-09 23:00:00  
> **类型**: 代码规范  
> **状态**: ✅ 生效中  
> **优先级**: 🔥 必须遵守

---

## ⚠️ 重要：每个新 Session 必读

**在任何新的 Copilot Session 中开始编码前，请告诉 Copilot**：
```
"遵守 20251209-23-00-00-CODE_STANDARDS.md 中的所有代码规范"
```

或者：
```
"按照 RAG 2.0 代码规范开发"
```

**Copilot 会自动**：
1. ✅ 读取本文档
2. ✅ 应用所有代码规范
3. ✅ 生成符合规范的代码

---

## 📂 文档管理规范

### 1. 目录结构

```
docs/refactor/
├── 20251209-23-00-00-CODE_STANDARDS.md          (本文档 - 代码规范)
├── 20251209-22-29-00-IMPLEMENTATION_PLAN.md     (主实施计划 - 持续更新)
├── SESSION_RECOVERY_GUIDE.md                     (会话恢复指南)
├── README.md                                     (目录说明)
│
├── phase-1/
│   └── 20251210-xx-xx-Phase1-Progress.md         (Phase 1 进度 - 持续更新)
│
├── phase-2/
│   └── 20251211-xx-xx-Phase2-Progress.md         (Phase 2 进度 - 持续更新)
│
├── ...
│
└── done/                                         (已完成的阶段性报告)
    ├── 20251215-xx-xx-Phase1-Complete.md         (Phase 1 完成报告)
    ├── 20251220-xx-xx-Phase2-Complete.md         (Phase 2 完成报告)
    └── ...
```

### 2. 文档更新策略

#### 规则 A: 进度始终在同一个文档更新
```yaml
❌ 错误做法:
  - Phase1-Progress-Day1.md
  - Phase1-Progress-Day2.md
  - Phase1-Progress-Day3.md
  # 进度分散，难以追踪

✅ 正确做法:
  - phase-1/20251210-10-00-00-Phase1-Progress.md
    ├─ 2025-12-10 10:00 - 开始 Phase 1
    ├─ 2025-12-10 14:30 - 完成任务 1.1
    ├─ 2025-12-11 09:00 - 完成任务 1.2
    └─ 2025-12-12 16:00 - Phase 1 完成
  # 所有进度在一个文档中，清晰可追溯
```

#### 规则 B: 阶段完成时生成报告
```yaml
触发条件:
  - Phase X 的所有任务都完成
  - 验收标准全部通过
  
生成报告:
  文件: done/yyyyMMdd-HH-mm-ss-PhaseX-Complete.md
  内容:
    - 完成的功能列表
    - 代码统计
    - 测试结果
    - 遇到的问题和解决方案
    - 经验教训
    - 下一步建议
```

#### 规则 C: 文档命名规范
```yaml
持续更新的文档:
  格式: phase-{N}/yyyyMMdd-HH-mm-ss-Phase{N}-Progress.md
  示例: phase-1/20251210-10-00-00-Phase1-Progress.md
  
完成报告:
  格式: done/yyyyMMdd-HH-mm-ss-Phase{N}-Complete.md
  示例: done/20251215-16-30-00-Phase1-Complete.md
  
其他文档:
  格式: yyyyMMdd-HH-mm-ss-{描述}.md
  示例: 20251209-23-00-00-CODE_STANDARDS.md
```

---

## 💻 代码规范

### 1. 注释格式规范

#### 规则 1.1: 类注释
```java
/**
 * 角色检测器 (Role Detector)
 * 
 * 负责分析用户问题，识别所属角色
 * (Responsible for analyzing user questions and identifying roles)
 * 
 * @author AI Reviewer Team
 * @since 2.0.0
 */
public class RoleDetector {
    // ...
}
```

#### 规则 1.2: 方法注释
```java
/**
 * 检测问题所属的角色 (Detect roles for the question)
 * 
 * @param question 用户问题 (User question)
 * @param context 上下文信息 (Context information)
 * @return 角色评分列表 (List of role scores)
 */
public List<RoleScore> detect(String question, String context) {
    // 实现逻辑...
}
```

#### 规则 1.3: 行内注释
```java
// 1. 提取关键词 (Extract keywords)
List<String> keywords = extractKeywords(question);

// 2. 计算相似度 (Calculate similarity)
double similarity = calculateSimilarity(keywords, roleKeywords);

// 3. 返回评分 (Return score)
return new RoleScore(roleId, similarity);
```

#### 规则 1.4: 复杂逻辑注释
```java
/**
 * 融合多个角色的检索结果
 * (Fuse search results from multiple roles)
 * 
 * 算法说明 (Algorithm description):
 * 1. 收集所有角色的检索结果
 * 2. 对相同文档的分数进行加权累加
 * 3. 按最终分数排序
 * 4. 返回 Top K 结果
 */
private List<Document> fuseResults(List<RoleSearchResult> results, int topK) {
    // ...
}
```

---

### 2. Lombok 注解规范

#### ⭐ 核心原则：优先使用 @Data
```
默认规则：所有 POJO/DTO/VO/实体类都使用 @Data 注解
例外情况：只有在特殊业务逻辑或安全需求时才手动编写 getter/setter
```

#### 规则 2.1: 使用 @Data 简化 POJO
```java
// ❌ 错误做法 - 手动编写 getter/setter
public class UserInfo {
    private String userId;
    private String userName;
    private int age;
    
    public String getUserId() {
        return userId;
    }
    
    public void setUserId(String userId) {
        this.userId = userId;
    }
    
    public String getUserName() {
        return userName;
    }
    
    public void setUserName(String userName) {
        this.userName = userName;
    }
    
    // ... 更多 getter/setter
}

// ✅ 正确做法 - 使用 @Data 注解
import lombok.Data;

@Data
public class UserInfo {
    private String userId;
    private String userName;
    private int age;
}
```

#### 规则 2.2: 使用 @Getter/@Setter 精确控制
```java
// 场景：只需要 Getter，不需要 Setter
import lombok.Getter;

@Getter
public class ImmutableConfig {
    private final String configKey;
    private final String configValue;
    
    public ImmutableConfig(String key, String value) {
        this.configKey = key;
        this.configValue = value;
    }
}

// 场景：部分字段需要自定义 Setter 逻辑
import lombok.Getter;
import lombok.Setter;

@Getter
public class UserProfile {
    @Setter
    private String nickname;
    
    private String email; // 只读
    
    // 自定义 Setter 逻辑 (Custom setter logic)
    public void setEmail(String email) {
        if (email != null && email.contains("@")) {
            this.email = email.toLowerCase();
        }
    }
}
```

#### 规则 2.3: 使用 @Builder 构建复杂对象
```java
import lombok.Builder;
import lombok.Data;

@Data
@Builder
public class SearchRequest {
    private String query;
    private String userId;
    private List<String> roles;
    private int topK;
    private double threshold;
}

// 使用示例 (Usage example)
SearchRequest request = SearchRequest.builder()
    .query("如何使用Spring Boot")
    .userId("user123")
    .roles(Arrays.asList("developer", "architect"))
    .topK(10)
    .threshold(0.7)
    .build();
```

#### 规则 2.4: 内部类使用 Lombok
```java
// ✅ 推荐：聚合结果内部类使用 @Data
import lombok.Data;

public class SignalAggregator {
    
    @Data
    public static class UserAggregation {
        private String userId;
        private AttitudeScore averageAttitude;
        private Map<SignalType, Long> signalTypeDistribution;
        private long positiveSignalCount;
        private long negativeSignalCount;
        private String tendency;
        private String attitudeTrend;
        
        public UserAggregation(String userId) {
            this.userId = userId;
        }
    }
    
    @Data
    public static class ConceptAggregation {
        private String answerId;
        private double averageScore;
        private long positiveFeedbackCount;
        private long negativeFeedbackCount;
        private double feedbackRatio;
        private boolean problematic;
        private double confidenceIndex;
        
        public ConceptAggregation(String answerId) {
            this.answerId = answerId;
        }
    }
}
```

#### 规则 2.5: Lombok 注解选择指南
```yaml
默认规则 - 优先使用 @Data:
  ✅ 适用于 99% 的场景:
    - 所有 DTO/VO/POJO
    - 内部数据类
    - 聚合结果类
    - 配置类
    - 请求/响应对象
    - 实体类（Entity）
    - 数据模型类
  
  使用方式:
    - 简单类：直接使用 @Data
    - 需要构造函数：@Data + 自定义构造函数
    - 不可变对象：@Data + final 字段

特殊场景 - 手动编写 getter/setter:
  ⚠️ 仅在以下情况手动编写:
    
    1. 安全需求（字段脱敏）:
       @Getter
       private String password;
       
       // 手动编写 setter 进行加密
       public void setPassword(String password) {
           this.password = encrypt(password);
       }
    
    2. 数据验证:
       @Getter
       private String email;
       
       // 手动编写 setter 进行验证
       public void setEmail(String email) {
           if (!email.contains("@")) {
               throw new IllegalArgumentException("Invalid email");
           }
           this.email = email.toLowerCase();
       }
    
    3. 数据格式化:
       @Getter
       private BigDecimal price;
       
       // 手动编写 getter 进行格式化
       public String getFormattedPrice() {
           return String.format("$%.2f", price);
       }
    
    4. 延迟加载:
       private List<Item> items;
       
       public List<Item> getItems() {
           if (items == null) {
               items = loadItems();
           }
           return items;
       }

其他注解配合使用:
  @Builder:
    - 参数较多的对象（>3个）
    - 可与 @Data 同时使用
  
  @AllArgsConstructor / @NoArgsConstructor:
    - JPA 实体类要求
    - 可与 @Data 同时使用
  
  @ToString / @EqualsAndHashCode:
    - @Data 已包含，一般不需要单独使用
    - 除非需要自定义排除字段
```

#### 规则 2.6: Lombok 与其他注解配合
```java
// ✅ 与 JPA 配合
import lombok.Data;
import javax.persistence.*;

@Data
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false)
    private String username;
    
    @Column(nullable = false)
    private String email;
}

// ✅ 与 Jackson 配合
import lombok.Data;
import com.fasterxml.jackson.annotation.JsonProperty;

@Data
public class ApiResponse {
    @JsonProperty("status_code")
    private int statusCode;
    
    @JsonProperty("error_message")
    private String errorMessage;
    
    private Object data;
}
```

---

### 3. 日志规范

#### 规则 3.1: 日志级别
```java
// DEBUG - 详细的调试信息 (Detailed debugging information)
log.debug("角色检测: 关键词={}, 匹配数={}", keywords, matchCount);

// INFO - 重要的业务流程 (Important business process)
log.info("开始角色检测: question={}", question);

// WARN - 警告信息，但不影响正常流程 (Warning, but not affect normal process)
log.warn("角色检测置信度较低: score={}", score);

// ERROR - 错误信息，需要关注 (Error, need attention)
log.error("角色检测失败: question={}, error={}", question, e.getMessage(), e);
```

#### 规则 3.2: 日志国际化
```java
// ❌ 错误做法 - 日志硬编码
log.info("开始处理用户请求");

// ✅ 正确做法 - 使用国际化
log.info(I18n.get("role.detector.start"), question);
```

#### 规则 3.3: 国际化检查与兼容性
```yaml
国际化检查清单:
  1. 新增国际化键值时:
     - 必须在所有语言文件中添加对应键值
     - 保持键值名称和参数个数完全一致
     - 使用语义化的键名，如: module.action.description
  
  2. 更新国际化文件时:
     - 检查是否影响到现有功能的翻译
     - 确保没有遗漏的语言文件
     - 验证参数占位符 {0}, {1} 的一致性
  
  3. 代码中使用国际化:
     - 避免直接拼接国际化结果
     - 传递参数时保持类型匹配
     - 复杂逻辑应考虑在国际化文件中处理

国际化文件结构:
  路径: src/main/resources/i18n/{语言}/
  文件命名: {语言}-{模块}.yml
  示例:
    - en-hope.yml (HOPE模块英文本地化)
    - zh-hope.yml (HOPE模块中文本地化)
```

---

### 4. 常量字符串国际化

#### 规则 4.1: 常量定义
```java
// ❌ 错误做法 - 硬编码字符串
public class RoleDetector {
    private static final String ERROR_MSG = "角色检测失败";
    
    public void detect() {
        throw new RuntimeException(ERROR_MSG);
    }
}

// ✅ 正确做法 - 使用国际化键
public class RoleDetector {
    public void detect() {
        throw new RuntimeException(I18n.get("role.detector.error"));
    }
}
```

#### 规则 4.2: YAML 文件结构
```yaml
文件位置: src/main/resources/i18n/

目录结构（按语言分目录）:
  src/main/resources/i18n/
    ├── zh/                          (中文目录)
    │   ├── zh_messages.yml          (老代码兼容 - messages_zh.yml)
    │   ├── zh-common.yml            (通用消息)
    │   ├── zh-role-detector.yml     (角色检测模块)
    │   ├── zh-vector-index.yml      (向量索引模块)
    │   ├── zh-concept-evolution.yml (概念演化模块)
    │   ├── zh-feedback.yml          (反馈模块)
    │   └── zh-error.yml             (错误消息)
    │
    └── en/                          (英文目录)
        ├── en_messages.yml          (老代码兼容 - messages_en.yml)
        ├── en-common.yml            (通用消息)
        ├── en-role-detector.yml     (角色检测模块)
        ├── en-vector-index.yml      (向量索引模块)
        ├── en-concept-evolution.yml (概念演化模块)
        ├── en-feedback.yml          (反馈模块)
        └── en-error.yml             (错误消息)

命名规范:
  - 文件按语言分目录存放，便于管理
  - 文件名保留语言前缀（zh-/en-），便于识别
  - 特殊文件：zh_messages.yml 和 en_messages.yml 用于兼容老代码
```

#### 规则 3.3: YAML 内容格式

**中文文件示例** (`zh/zh-role-detector.yml`):
```yaml
# 中文角色检测消息
# 文件: src/main/resources/i18n/zh/zh-role-detector.yml

role:
  detector:
    start: "🚀 开始角色检测: question={0}"
    success: "✅ 角色检测成功: role={0}, confidence={1}"
    failed: "❌ 角色检测失败: {0}"
    keyword-match: "🔍 关键词匹配: keywords={0}, matchCount={1}"
    ai-analysis: "🤖 AI 智能分析中..."
```

**英文文件示例** (`en/en-role-detector.yml`):
```yaml
# English Role Detector Messages
# File: src/main/resources/i18n/en/en-role-detector.yml

role:
  detector:
    start: "🚀 Starting role detection: question={0}"
    success: "✅ Role detection succeeded: role={0}, confidence={1}"
    failed: "❌ Role detection failed: {0}"
    keyword-match: "🔍 Keyword matching: keywords={0}, matchCount={1}"
    ai-analysis: "🤖 AI analyzing..."
```

**关键特性**:
- ✅ 中英文完全分开，便于管理
- ✅ 文件名用 `zh-` 和 `en-` 前缀区分
- ✅ 使用 `{0}`, `{1}` 作为参数占位符（MessageFormat 格式）
- ✅ 支持 Emoji，提升日志可读性

---

### 4. 代码组织规范

#### 规则 4.1: 包结构
```
top.yumbo.ai.rag
├── role/                      (角色相关)
│   ├── Role.java
│   ├── RoleConfig.java
│   ├── RoleDetector.java
│   └── RoleManager.java
│
├── concept/                   (概念相关)
│   ├── ConceptUnit.java
│   ├── ConceptVersion.java
│   ├── ConceptUpdater.java
│   └── VersionManager.java
│
├── index/                     (索引相关)
│   ├── RoleVectorIndex.java
│   ├── DocumentClassifier.java
│   └── IndexBuilder.java
│
├── retriever/                 (检索相关)
│   ├── MultiRoleRetriever.java
│   ├── ResultFusion.java
│   └── WeightCalculator.java
│
└── i18n/                      (国际化)
    ├── I18n.java
    └── MessageLoader.java
```

#### 规则 4.2: 类命名
```java
// 服务类 (Service classes)
RoleDetectorService
ConceptEvolutionService

// 管理类 (Manager classes)
RoleManager
VersionManager

// 仓储类 (Repository classes)
ConceptRepository
RoleRepository

// 工具类 (Utility classes)
RoleUtils
ConceptUtils

// 配置类 (Configuration classes)
RoleConfig
IndexConfig
```

---

### 5. 国际化工具类

#### 规则 5.1: I18n 工具类实现

**已实现**: `src/main/java/top/yumbo/ai/rag/i18n/I18N.java`

**核心特性**:
- ✅ 静态自动加载：应用启动时自动加载 `i18n/zh/` 和 `i18n/en/` 目录下的所有 YAML 文件
- ✅ 目录分离：中文文件放在 `zh/` 目录，英文文件放在 `en/` 目录
- ✅ 中英文分离：分别加载到 `messagesZh` 和 `messagesEn` 两个 Map
- ✅ 老代码兼容：自动加载 `messages` 模块（对应 `zh_messages.yml` 和 `en_messages.yml`）
- ✅ 智能语言检测：根据系统属性/环境变量/默认语言自动选择
- ✅ 参数格式化：使用 `MessageFormat` 支持 `{0}`, `{1}` 占位符
- ✅ 降级处理：找不到 key 时返回 `[key]` 而不是抛异常

**加载逻辑**:
```java
static {
    // 加载中文消息 (Load Chinese messages)
    // 扫描 i18n/zh/ 目录下的所有文件 (Scan all files in i18n/zh/)
    loadMessagesWithPrefix("i18n/zh/zh-", messagesZh, "Chinese");
    
    // 加载英文消息 (Load English messages)
    // 扫描 i18n/en/ 目录下的所有文件 (Scan all files in i18n/en/)
    loadMessagesWithPrefix("i18n/en/en-", messagesEn, "English");
}
```

**使用方法**:
```java
// 自动根据系统语言选择中英文 (Auto select language)
log.info(I18N.get("role.detector.start", question));

// 指定语言（用于 API 响应） (Specify language for API)
String msg = I18N.getLang("error.file.not-found", "en", filename);
```

**预定义模块**:
系统会自动加载以下模块的国际化文件：
- `messages` - 老代码兼容（zh_messages.yml / en_messages.yml）
- `common` - 通用消息
- `role-detector` - 角色检测
- `vector-index` - 向量索引
- `concept-evolution` - 概念演化
- `feedback` - 反馈系统
- `retriever` - 检索器
- `streaming` - 流式响应
- `error` - 错误消息


---

### 6. 使用示例

#### 示例 6.1: 创建 YAML 文件

**中文文件** (`zh/zh-role-detector.yml`):
```yaml
# 中文角色检测消息
# 文件: src/main/resources/i18n/zh/zh-role-detector.yml

role:
  detector:
    start: "🚀 开始角色检测: question={0}"
    success: "✅ 角色检测成功: role={0}, confidence={1}"
    failed: "❌ 角色检测失败: {0}"
    keyword-match: "🔍 关键词匹配: keywords={0}, count={1}"
    ai-analysis: "🤖 AI 智能分析中..."
    low-confidence: "⚠️ 置信度较低: score={0}"
```

**英文文件** (`en/en-role-detector.yml`):
```yaml
# English Role Detector Messages
# File: src/main/resources/i18n/en/en-role-detector.yml

role:
  detector:
    start: "🚀 Starting role detection: question={0}"
    success: "✅ Role detection succeeded: role={0}, confidence={1}"
    failed: "❌ Role detection failed: {0}"
    keyword-match: "🔍 Keyword matching: keywords={0}, count={1}"
    ai-analysis: "🤖 AI analyzing..."
    low-confidence: "⚠️ Low confidence: score={0}"
```

#### 示例 6.2: 在代码中使用
```java
package top.yumbo.ai.rag.role;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import top.yumbo.ai.rag.i18n.I18N;

import java.util.List;

/**
 * 角色检测器 (Role Detector)
 * 
 * 负责分析用户问题，识别所属角色
 * (Responsible for analyzing user questions and identifying roles)
 * 
 * @author AI Reviewer Team
 * @since 2.0.0
 */
@Slf4j
@Service
public class RoleDetector {
    
    /**
     * 检测角色 (Detect role)
     * 
     * @param question 用户问题 (User question)
     * @return 角色评分 (Role score)
     */
    public RoleScore detect(String question) {
        // 开始检测 (Start detection)
        log.info(I18N.get("role.detector.start", question));
        
        try {
            // 1. 关键词匹配 (Keyword matching)
            List<String> keywords = extractKeywords(question);
            int matchCount = countMatches(keywords);
            log.debug(I18N.get("role.detector.keyword-match", keywords, matchCount));
            
            // 2. 计算评分 (Calculate score)
            double score = (double) matchCount / keywords.size();
            
            // 3. 检查置信度 (Check confidence)
            if (score < 0.5) {
                log.warn(I18N.get("role.detector.low-confidence", score));
            }
            
            // 4. 返回结果 (Return result)
            RoleScore result = new RoleScore("developer", score);
            log.info(I18N.get("role.detector.success", result.getRoleId(), score));
            
            return result;
            
        } catch (Exception e) {
            log.error(I18N.get("role.detector.failed", e.getMessage()), e);
            throw new RuntimeException(I18N.get("role.detector.failed", e.getMessage()), e);
        }
    }
    
    // ...其他方法...
}
```

**关键点**:
- ✅ 无需手动加载文件，`I18N` 类在静态初始化时已自动加载所有 `zh-*.yml` 和 `en-*.yml` 文件
- ✅ 使用 `I18N.get(key, args...)` 获取消息，系统自动根据语言环境选择中英文
- ✅ 参数使用 `{0}`, `{1}` 占位符，自动格式化

---

## 🎨 前端代码规范

### 核心原则：以 JSX 为主实现前端组件

**⭐ 重要规则 ⭐**: 所有新的前端代码必须使用 **JSX 格式**实现，避免使用纯 JavaScript 创建 DOM 元素。

#### 为什么选择 JSX？

1. **可读性更强**: JSX 的标签语法更接近 HTML，易于理解和维护
2. **类型安全**: 配合 React，JSX 提供更好的类型检查
3. **组件化**: JSX 天然支持组件化开发，便于复用
4. **生态完善**: React 生态系统成熟，工具链完善
5. **团队协作**: 统一的代码风格，降低沟通成本

---

### 规则 6: JSX 优先规范

#### 规则 6.1: 必须使用 JSX 实现新组件

**❌ 错误做法** - 使用纯 JavaScript 创建 DOM:
```javascript
// 不推荐：使用纯 JavaScript 创建元素
function createButton() {
    const button = document.createElement('button');
    button.className = 'btn btn-primary';
    button.textContent = 'Click Me';
    button.onclick = handleClick;
    return button;
}

// 不推荐：使用字符串拼接 HTML
function createCard() {
    return `
        <div class="card">
            <h3>${title}</h3>
            <p>${content}</p>
        </div>
    `;
}
```

**✅ 正确做法** - 使用 JSX:
```jsx
// 推荐：使用 JSX 创建组件
function Button({ onClick, children }) {
    return (
        <button className="btn btn-primary" onClick={onClick}>
            {children}
        </button>
    );
}

// 推荐：JSX 组件清晰易读
function Card({ title, content }) {
    return (
        <div className="card">
            <h3>{title}</h3>
            <p>{content}</p>
        </div>
    );
}
```

#### 规则 6.2: JSX 文件命名和组织

**文件命名规范**:
```yaml
组件文件:
  格式: ComponentName.jsx
  位置: src/main/resources/static/js/components/
  
示例:
  ✅ WelcomeGuide.jsx          (引导页面组件)
  ✅ DualTrackAnswer.jsx       (双轨答案组件)
  ✅ HOPEDashboardPanel.jsx    (HOPE 仪表盘组件)
  ❌ welcome-guide.js          (错误：不是 JSX，命名不规范)
  ❌ welcomeGuide.jsx          (错误：应使用 PascalCase)

目录结构:
  src/main/resources/static/js/components/
    ├── common/                (通用组件)
    │   ├── Button.jsx
    │   ├── Modal.jsx
    │   └── LanguageContext.js (Context 可以是 .js)
    ├── tabs/                  (Tab 组件)
    │   ├── QATab.jsx
    │   ├── DocumentsTab.jsx
    │   └── StatisticsTab.jsx
    ├── WelcomeGuide.jsx       (顶层组件)
    ├── DualTrackAnswer.jsx
    └── App.jsx                (主应用)
```

#### 规则 6.3: JSX 组件结构规范

**标准 JSX 组件结构**:
```jsx
/**
 * 组件名称 (Component Name)
 * 功能描述 (Function description)
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */

// 1. 导入 React 和 Hooks (Import React and Hooks)
const { useState, useEffect, useCallback } = React;

// 2. 定义组件 (Define component)
function MyComponent({ prop1, prop2, onAction }) {
    // 3. 状态定义 (State definition)
    const [state1, setState1] = useState(initialValue);
    const [state2, setState2] = useState(null);
    
    // 4. 国际化 (Internationalization)
    const { t } = window.LanguageModule.useTranslation();
    
    // 5. 副作用 (Side effects)
    useEffect(() => {
        // 初始化逻辑 (Initialization logic)
        loadData();
    }, []);
    
    // 6. 事件处理函数 (Event handlers)
    const handleClick = useCallback(() => {
        // 处理点击 (Handle click)
        setState1(newValue);
        onAction?.();
    }, [onAction]);
    
    // 7. 辅助函数 (Helper functions)
    const formatData = (data) => {
        // 格式化数据 (Format data)
        return data.toString();
    };
    
    // 8. 渲染 (Render)
    return (
        <div className="my-component">
            {/* 组件内容 (Component content) */}
            <h2>{t('myTitle')}</h2>
            <button onClick={handleClick}>
                {t('myButton')}
            </button>
        </div>
    );
}

// 9. 导出到全局 (Export to global)
window.MyComponent = MyComponent;
```

#### 规则 6.4: JSX 代码风格

**缩进和格式**:
```jsx
// ✅ 正确：清晰的缩进和换行
function MyComponent() {
    return (
        <div className="container">
            <header>
                <h1>Title</h1>
            </header>
            <main>
                <p>Content</p>
            </main>
        </div>
    );
}

// ❌ 错误：所有代码挤在一行
function MyComponent() {
    return <div className="container"><header><h1>Title</h1></header><main><p>Content</p></main></div>;
}
```

**条件渲染**:
```jsx
// ✅ 推荐：使用 && 操作符
{isLoading && <LoadingSpinner />}
{error && <ErrorMessage message={error} />}

// ✅ 推荐：使用三元运算符
{isLoggedIn ? <Dashboard /> : <LoginForm />}

// ✅ 推荐：复杂条件提取为函数
const renderContent = () => {
    if (isLoading) return <LoadingSpinner />;
    if (error) return <ErrorMessage message={error} />;
    if (data) return <DataView data={data} />;
    return <EmptyState />;
};

return (
    <div className="container">
        {renderContent()}
    </div>
);
```

**列表渲染**:
```jsx
// ✅ 正确：使用 map 和 key
<ul>
    {items.map(item => (
        <li key={item.id}>
            {item.name}
        </li>
    ))}
</ul>

// ❌ 错误：没有 key 属性
<ul>
    {items.map(item => (
        <li>{item.name}</li>
    ))}
</ul>

// ❌ 错误：使用 index 作为 key（如果列表会变化）
<ul>
    {items.map((item, index) => (
        <li key={index}>{item.name}</li>
    ))}
</ul>
```

#### 规则 6.5: Props 和 State 管理

**Props 解构**:
```jsx
// ✅ 推荐：在参数中解构 props
function MyComponent({ title, content, onSave, isEditing = false }) {
    return (
        <div>
            <h2>{title}</h2>
            <p>{content}</p>
            {isEditing && <button onClick={onSave}>Save</button>}
        </div>
    );
}

// ❌ 不推荐：在组件内部解构
function MyComponent(props) {
    const { title, content, onSave, isEditing } = props;
    // ...
}
```

**State 命名**:
```jsx
// ✅ 推荐：清晰的命名
const [isLoading, setIsLoading] = useState(false);
const [documents, setDocuments] = useState([]);
const [selectedDocument, setSelectedDocument] = useState(null);
const [showModal, setShowModal] = useState(false);

// ❌ 不推荐：不清晰的命名
const [flag, setFlag] = useState(false);
const [data, setData] = useState([]);
const [item, setItem] = useState(null);
```

#### 规则 6.6: 事件处理

**事件处理命名**:
```jsx
// ✅ 推荐：使用 handle 前缀
const handleClick = () => { /* ... */ };
const handleSubmit = () => { /* ... */ };
const handleChange = (e) => { /* ... */ };
const handleDelete = (id) => { /* ... */ };

// ✅ 推荐：传递给子组件用 on 前缀
<ChildComponent
    onClick={handleClick}
    onSubmit={handleSubmit}
    onDelete={handleDelete}
/>
```

**避免内联函数**:
```jsx
// ❌ 不推荐：内联函数（每次渲染都创建新函数）
<button onClick={() => handleDelete(item.id)}>Delete</button>

// ✅ 推荐：使用 useCallback
const handleDeleteClick = useCallback(() => {
    handleDelete(item.id);
}, [item.id]);

<button onClick={handleDeleteClick}>Delete</button>

// ✅ 推荐：简单场景可以接受内联（如切换布尔值）
<button onClick={() => setShowModal(true)}>Open</button>
```

#### 规则 6.7: 注释和文档

**组件文档注释**:
```jsx
/**
 * 文档上传组件 (Document Upload Component)
 * 
 * 功能：支持拖拽上传、批量上传、文件类型限制
 * (Features: Drag & drop, batch upload, file type restriction)
 * 
 * @param {string} uploadUrl - 上传 API 地址 (Upload API URL)
 * @param {Function} onSuccess - 上传成功回调 (Success callback)
 * @param {Function} onError - 上传失败回调 (Error callback)
 * @param {Array<string>} acceptedTypes - 允许的文件类型 (Accepted file types)
 * @param {number} maxSize - 最大文件大小（MB）(Max file size in MB)
 * 
 * @example
 * <DocumentUpload
 *     uploadUrl="/api/documents/upload"
 *     onSuccess={handleUploadSuccess}
 *     onError={handleUploadError}
 *     acceptedTypes={['.pdf', '.docx']}
 *     maxSize={100}
 * />
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */
function DocumentUpload({ uploadUrl, onSuccess, onError, acceptedTypes, maxSize }) {
    // ...实现...
}
```

**代码注释**:
```jsx
// ✅ 推荐：关键逻辑添加中英文注释
function MyComponent() {
    // 初始化状态 (Initialize state)
    const [data, setData] = useState([]);
    
    // 加载数据 (Load data)
    useEffect(() => {
        // 发送请求到后端 (Send request to backend)
        loadData().then(result => {
            // 过滤无效数据 (Filter invalid data)
            const validData = result.filter(item => item.isValid);
            setData(validData);
        });
    }, []);
    
    // ...existing code...
}
```

#### 规则 6.8: 兼容性考虑

**使用 React.createElement 作为后备**:

当不能使用 JSX 语法时（如某些动态场景），使用 `React.createElement`:

```javascript
// 场景：动态创建组件
function renderComponent(componentName, props) {
    // ✅ 使用 React.createElement
    return React.createElement(
        window[componentName],
        props,
        props.children
    );
}

// 使用示例
const element = renderComponent('Button', {
    className: 'btn-primary',
    onClick: handleClick,
    children: 'Click Me'
});
```

**在 HTML 中引入 JSX 组件**:
```html
<!-- index.html -->
<!-- 1. 引入 Babel standalone 以支持 JSX -->
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

<!-- 2. JSX 文件必须使用 type="text/babel" -->
<script type="text/babel" src="js/components/MyComponent.jsx"></script>

<!-- 3. 启动应用 -->
<script type="text/babel">
    ReactDOM.render(
        <App />,
        document.getElementById('root')
    );
</script>
```

---

### 7. 样式管理规范

#### 规则 7.1: 样式文件组织

**原则**: 所有样式必须提取到独立的 CSS 文件中，禁止使用内联样式（特殊情况除外）

```yaml
样式文件位置: src/main/resources/static/assets/css/

目录结构:
  src/main/resources/static/assets/css/
    ├── reset.css              (重置样式)
    ├── main.css               (全局样式)
    ├── qa-tab.css             (问答 Tab)
    ├── documents-tab.css      (文档管理 Tab)
    ├── statistics-tab.css     (统计 Tab)
    ├── ai-analysis.css        (AI 分析面板)
    ├── hope-dashboard.css     (HOPE 仪表盘)
    ├── welcome-guide.css      (引导页面)
    └── ...                    (其他模块样式)

命名规范:
  - 文件名用小写字母和连字符 (kebab-case)
  - 一个模块一个 CSS 文件
  - 文件名与组件名对���
```

#### 规则 7.2: 禁止内联样式

**❌ 错误做法** - 使用内联样式:
```jsx
// 不推荐：内联样式难以维护和复用
<div style={{
    padding: '10px 15px',
    margin: '10px 0',
    background: 'linear-gradient(135deg, #667eea15 0%, #764ba215 100%)',
    border: '1px solid #667eea40',
    borderRadius: '8px',
    fontSize: '14px',
    color: '#667eea'
}}>
    提示信息
</div>
```

**✅ 正确做法** - 使用 CSS 类:
```jsx
// 推荐：使用 CSS 类，易于维护和复用
<div className="filter-hint">
    提示信息
</div>
```

**CSS 文件** (`documents-tab.css`):
```css
/* 实时过滤提示样式 (Real-time Filter Hint) */
.filter-hint {
    padding: 10px 15px;
    margin: 10px 0;
    background: linear-gradient(135deg, #667eea15 0%, #764ba215 100%);
    border: 1px solid #667eea40;
    border-radius: 8px;
    font-size: 14px;
    color: #667eea;
}
```

#### 规则 7.3: 允许内联样式的特殊情况

**仅在以下情况允许使用内联样式**:

1. **动态计算的值** (需要根据 props 或 state 动态改变)
   ```jsx
   <div style={{ width: `${progress}%` }}>进度条</div>
   <div style={{ left: `${position}px` }}>拖拽元素</div>
   ```

2. **一次性使用的临时样式** (调试或 POC 阶段)
   ```jsx
   // ⚠️ 仅用于快速原型，最终必须提取到 CSS
   <div style={{ border: '1px solid red' }}>调试边框</div>
   ```

3. **第三方库要求的样式** (某些库必须通过 style prop 传递)
   ```jsx
   <ThirdPartyComponent style={requiredStyles} />
   ```

**注意**: 即使使用内联样式，也应在代码审查时评估是否可以提取到 CSS。

#### 规则 7.4: CSS 类命名规范

**使用 BEM 命名法** (Block-Element-Modifier):

```css
/* Block (块) */
.documents-filter-hint { }

/* Element (元素) */
.documents-filter-hint__icon { }
.documents-filter-hint__message { }

/* Modifier (修饰符) */
.documents-filter-hint--warning { }
.documents-filter-hint--error { }
.documents-filter-hint--success { }
```

**命名规则**:
- 使用小写字母和连字符 (kebab-case)
- 类名应具有语义化，描述用途而非样式
- 避免使用缩写（除非是通用缩写如 btn、nav）
- 模块前缀 + 功能描述

**示例**:
```css
/* ✅ 好的命名 */
.documents-upload-area { }
.search-filter-container { }
.ai-analysis-panel { }

/* ❌ 不好的命名 */
.blue-box { }          /* 描述样式而非用途 */
.div1 { }              /* 无意义 */
.temp { }              /* 过于笼统 */
```

#### 规则 7.5: CSS 文件结构

**每个 CSS 文件应遵循统一的结构**:

```css
/**
 * 模块名称样式文件 (Module Name Styles)
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */

/* ============================================================================
   全局样式 (Global Styles)
   ============================================================================ */
.module-container {
    /* ... */
}

/* ============================================================================
   头部样式 (Header Styles)
   ============================================================================ */
.module-header {
    /* ... */
}

/* ============================================================================
   内容区域样式 (Content Area Styles)
   ============================================================================ */
.module-content {
    /* ... */
}

/* ============================================================================
   底部样式 (Footer Styles)
   ============================================================================ */
.module-footer {
    /* ... */
}

/* ============================================================================
   响应式样式 (Responsive Styles)
   ============================================================================ */
@media (max-width: 768px) {
    .module-container {
        /* 移动端样式 */
    }
}
```

**结构规范**:
- ✅ 文件头部包含注释说明
- ✅ 使用分隔线将不同功能区域分组
- ✅ 每组样式有中英文注释
- ✅ 响应式样式放在文件末尾

#### 规则 7.6: 样式复用与变量

**使用 CSS 变量管理主题色**:

```css
/* 在 main.css 或 root 中定义全局变量 */
:root {
    /* 主题色 (Theme Colors) */
    --primary-color: #667eea;
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    
    /* 间距 (Spacing) */
    --spacing-xs: 5px;
    --spacing-sm: 10px;
    --spacing-md: 15px;
    --spacing-lg: 20px;
    
    /* 圆角 (Border Radius) */
    --radius-sm: 4px;
    --radius-md: 8px;
    --radius-lg: 12px;
}

/* 在具体样式中使用变量 */
.filter-hint {
    padding: var(--spacing-sm) var(--spacing-md);
    margin: var(--spacing-sm) 0;
    background: var(--primary-gradient);
    border-radius: var(--radius-md);
    color: var(--primary-color);
}
```

#### 规则 7.7: 动画规范

**动画应定义在 CSS 中，使用 @keyframes**:

```css
/* ✅ 正确：在 CSS 中定义动画 */
.filter-hint {
    animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-5px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

**避免在 JS 中操作样式**:
```javascript
// ❌ 错误：在 JS 中直接操作样式
element.style.opacity = '0';
element.style.transform = 'translateY(-5px)';

// ✅ 正确：使用 CSS 类切换
element.classList.add('fade-in');
```

---

## 📋 代码质量检查清单

### 每次提交代码前必须检查

#### 后端代码 (Backend Code)
- [ ] **注释格式**：所有类、方法、复杂逻辑都有 `中文(英文)` 注释
- [ ] **日志国际化**：所有日志使用 `I18n.get()` 而不是硬编码
- [ ] **常量提取**：所有字符串常量提取到 YAML 文件
- [ ] **YAML 文件**：已创建对应的 `{模块名}.yml` 文件
- [ ] **中英文版本**：YAML 文件包含中英文两个版本，用 `---` 分隔
- [ ] **编译通过**：`mvn compile` 无错误
- [ ] **测试通过**：相关单元测试通过
- [ ] **代码格式**：符合 Java 代码规范

#### 前端代码 (Frontend Code)
- [ ] **⭐ JSX 优先**：新组件必须使用 JSX 格式实现，避免纯 JavaScript 创建 DOM
- [ ] **文件命名**：组件文件使用 `.jsx` 扩展名，PascalCase 命名（如 `MyComponent.jsx`）
- [ ] **组件结构**：遵循标准 JSX 组件结构（imports → state → effects → handlers → render）
- [ ] **Props 解构**：在函数参数中解构 props，使用清晰的命名
- [ ] **事件处理**：事件处理函数使用 `handle` 前缀，传递给子组件用 `on` 前缀
- [ ] **条件渲染**：使用 `&&`、三元运算符或函数，避免复杂的内联逻辑
- [ ] **列表渲染**：使用 `map` 且必须提供 `key` 属性（使用唯一 ID，避免 index）
- [ ] **样式提取**：所有样式提取到 CSS 文件，避免内联样式
- [ ] **CSS 文件**：已创建或更新对应的 CSS 文件
- [ ] **类名规范**：CSS 类名使用 BEM 命名法，语义化命名
- [ ] **注释完整**：组件有文件头注释，关键逻辑有中英文注释
- [ ] **国际化**：前端文本使用 `t()` 函数，避免硬编码
- [ ] **动画定义**：动画在 CSS 中定义，使用 @keyframes
- [ ] **变量使用**：使用 CSS 变量管理主题色和常用值
- [ ] **响应式**：考虑移动端适配，添加媒体查询

---

## 🔄 新 Session 启动检查清单

### 每次打开新 Session 时

1. **告诉 Copilot 遵守规范**：
   ```
   "遵守 20251209-23-00-00-CODE_STANDARDS.md 中的所有代码规范"
   ```

2. **验证 Copilot 理解**：
   ```
   "生成一个示例类，展示正确的注释格式和国际化用法"
   ```

3. **确认规范已应用**：
   - ✅ 注释格式正确
   - ✅ 日志使用 I18n
   - ✅ 字符串已国际化

---

## 📚 相关文档

- **实施计划**：`20251209-22-29-00-IMPLEMENTATION_PLAN.md`
- **会话恢复**：`SESSION_RECOVERY_GUIDE.md`
- **目录说明**：`README.md`

---

## ✅ 规范版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.3 | 2025-12-10 21:20 | 🎯 新增规则 6：JSX 优先规范（6.1-6.8），明确前端代码必须使用 JSX 实现 |
| v1.2 | 2025-12-10 21:15 | 新增前端样式管理规范（规则 7.1-7.7），禁止内联样式，统一 CSS 管理 |
| v1.1 | 2025-12-09 23:30 | 优化文件组织：按语言分目录（zh/和en/），添加老代码兼容 |
| v1.0 | 2025-12-09 23:00 | 初始版本，建立代码规范 |

---

**这是 RAG 2.0 项目的核心规范，所有开发人员必须遵守！** 🎯

---

**文档版本**: v1.3  
**创建日期**: 2025-12-09 23:00:00  
**最后更新**: 2025-12-10 21:20:00  
**状态**: ✅ 生效中  
**维护者**: AI Reviewer Team

