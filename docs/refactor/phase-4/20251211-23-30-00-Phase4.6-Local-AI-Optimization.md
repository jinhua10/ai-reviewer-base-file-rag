# Phase 4.6 - 本地优先 AI 服务与愿望单系统
# Phase 4.6 - Local-First AI Services & Wish List System

> **文档编号**: 20251211-23-30-00  
> **创建日期**: 2025-12-11 23:30:00  
> **文档类型**: 技术方案设计  
> **优先级**: 🔥 高优先级  
> **目标**: 本地算力优化 + 业务数据利用 + AI 服务扩展

---

## 🎯 核心问题与解决方案

### 问题 1: 概念单元构建不够精准
```yaml
担忧:
  - GPT-5 等大模型可能对业务领域理解不够精准
  - 通用模型无法理解公司特定的术语和概念
  - 在线 API 延迟高、成本高

解决方案:
  方案A: 本地小模型 + 业务微调
    - 使用 Qwen2.5-7B / Llama3-8B 等开源小模型
    - 基于公司业务数据微调
    - 本地部署，低延迟，零成本
  
  方案B: 混合模式（本地 + 云端）
    - 日常任务：本地小模型（快速、免费）
    - 复杂任务：云端大模型（精准、按需）
    - 智能路由：根据任务复杂度自动选择
  
  方案C: 渐进式学习
    - 初期：使用 GPT-5 构建概念单元
    - 中期：收集用户反馈和修正
    - 后期：基于真实数据训练本地模型
    - 最终：本地模型准确率 > 云端模型
```

### 问题 2: 本地资源优化
```yaml
目标:
  - 同事使用时不占用过多资源
  - 充分利用闲置算力
  - 后台智能调度

解决方案:
  1. 异步处理架构:
     - 用户提问 → 立即返回缓存答案
     - 后台异步 → 生成/优化概念单元
     - 系统空闲时 → 批量处理
  
  2. 智能资源调度:
     - 工作时间（9:00-18:00）：
       * CPU 限制 20%
       * 只处理高优先级任务
     
     - 下班时间（18:00-9:00）：
       * CPU 限制 80%
       * 批量处理积压任务
       * 模型训练和优化
     
     - 周末/节假日：
       * CPU 限制 100%
       * 全力训练本地模型
       * 知识库优化
  
  3. 增量计算:
     - 只处理新增数据
     - 缓存计算结果
     - 避免重复计算
```

### 问题 3: 业务数据的价值
```yaml
核心洞察:
  同事使用 AI 服务产生的数据是业务强相关的！
  
  场景:
    - 生成 PPT：内容包含项目方案、技术架构
    - 生成报告：包含业务分析、数据总结
    - 代码生成：包含业务逻辑、接口定义
  
  → 这些数据是训练业务专属模型的黄金数据！

数据利用策略:
  1. 数据收集（用户授权）:
     - 用户生成内容
     - 用户修改记录（修正）
     - 用户反馈评分
  
  2. 数据标注（自动 + 人工）:
     - 自动提取关键概念
     - 识别业务术语
     - 构建领域词典
  
  3. 模型微调:
     - 定期（每周/每月）微调本地模型
     - 使用业务数据持续优化
     - 准确率逐步提升
  
  4. 知识库增强:
     - 提取业务概念单元
     - 完善角色知识库
     - 构建公司专属知识图谱
```

---

## 🏗️ 架构设计

### 1. 本地优先 AI 引擎架构

```
┌─────────────────────────────────────────────────────────┐
│                   用户接口层                             │
│         (User Interface Layer)                          │
│                                                          │
│  问答  生成PPT  生成报告  代码生成  图像理解  ...       │
└─────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│              智能路由层 (Intelligent Router)             │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  任务分析器 (Task Analyzer)                      │  │
│  │  - 复杂度评估                                    │  │
│  │  - 实时性要求                                    │  │
│  │  - 成本预算                                      │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────┐        ┌──────────────┐             │
│  │ 本地模型路由  │        │ 云端模型路由  │             │
│  │ (Local Route)│        │ (Cloud Route)│             │
│  └──────────────┘        └──────────────┘             │
└─────────────────────────────────────────────────────────┘
          ▼                           ▼
┌──────────────────┐        ┌──────────────────┐
│   本地模型引擎    │        │   云端模型引擎    │
│  (Local Engine)  │        │  (Cloud Engine)  │
│                  │        │                  │
│ Qwen2.5-7B       │        │ GPT-5            │
│ Llama3-8B        │        │ Claude 3.5       │
│ Phi-3-mini       │        │ Gemini Pro       │
│                  │        │                  │
│ 本地部署          │        │ API 调用         │
│ GPU/CPU 推理     │        │ 按需付费         │
│ 零成本           │        │ 高精度           │
└──────────────────┘        └──────────────────┘
          ▼                           ▼
┌─────────────────────────────────────────────────────────┐
│              结果缓存与优化层                             │
│         (Result Cache & Optimization)                   │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 结果缓存  │  │ 质量评分  │  │ 用户反馈  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
└─────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│           业务数据收集与模型训练                          │
│      (Business Data Collection & Model Training)        │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 数据收集      │  │ 自动标注      │  │ 模型微调      │ │
│  │ (下班时间)    │  │ (周末)        │  │ (深夜)        │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 2. 智能路由决策逻辑

```java
/**
 * AI 模型智能路由器 (AI Model Intelligent Router)
 */
public class AIModelRouter {
    
    /**
     * 路由决策 (Routing Decision)
     */
    public RouteDecision route(AITask task, UserContext context) {
        // 1. 任务复杂度评估 (Complexity assessment)
        TaskComplexity complexity = assessComplexity(task);
        
        // 2. 实时性要求 (Real-time requirement)
        boolean requiresRealtime = task.isRealtimeRequired();
        
        // 3. 成本预算 (Cost budget)
        double costBudget = context.getCostBudget();
        
        // 4. 本地模型能力评估 (Local model capability)
        double localConfidence = assessLocalCapability(task);
        
        // 决策逻辑 (Decision logic)
        if (requiresRealtime && localConfidence > 0.8) {
            // 实时要求 + 本地有把握 → 本地模型 (Realtime + Local confident → Local)
            return RouteDecision.LOCAL;
        }
        
        if (complexity == TaskComplexity.LOW && localConfidence > 0.7) {
            // 简单任务 + 本地可以 → 本地模型 (Simple task + Local can → Local)
            return RouteDecision.LOCAL;
        }
        
        if (complexity == TaskComplexity.HIGH && costBudget > 0) {
            // 复杂任务 + 有预算 → 云端模型 (Complex task + Budget → Cloud)
            return RouteDecision.CLOUD;
        }
        
        // 混合模式：本地先尝试，失败则云端 (Hybrid: Local first, Cloud fallback)
        return RouteDecision.HYBRID;
    }
    
    /**
     * 复杂度评估 (Complexity Assessment)
     */
    private TaskComplexity assessComplexity(AITask task) {
        // 1. 输入长度 (Input length)
        int inputLength = task.getInputLength();
        
        // 2. 任务类型 (Task type)
        TaskType type = task.getType();
        
        // 3. 领域匹配度 (Domain match)
        double domainMatch = calculateDomainMatch(task);
        
        if (type == TaskType.SIMPLE_QA && inputLength < 500 && domainMatch > 0.8) {
            return TaskComplexity.LOW; // 本地模型可以搞定 (Local can handle)
        }
        
        if (type == TaskType.CODE_GENERATION || inputLength > 2000) {
            return TaskComplexity.HIGH; // 需要云端大模型 (Need cloud LLM)
        }
        
        return TaskComplexity.MEDIUM; // 混合模式 (Hybrid mode)
    }
}
```

### 3. 资源调度系统

```java
/**
 * 智能资源调度器 (Intelligent Resource Scheduler)
 */
public class ResourceScheduler {
    
    /**
     * 根据时间和系统负载调度任务 (Schedule tasks based on time and load)
     */
    public void schedule() {
        LocalTime now = LocalTime.now();
        int hour = now.getHour();
        
        // 1. 工作时间策略 (Work hours strategy)
        if (hour >= 9 && hour < 18) {
            setResourceLimit(0.2);  // CPU 限制 20%
            priorityMode = Priority.REALTIME; // 只处理实时任务
            batchProcessing = false; // 暂停批处理
        }
        
        // 2. 下班时间策略 (After hours strategy)
        else if (hour >= 18 || hour < 9) {
            setResourceLimit(0.8);  // CPU 限制 80%
            priorityMode = Priority.BATCH; // 批量处理
            batchProcessing = true; // 启动批处理
            
            // 处理积压任务 (Process backlog)
            processPendingTasks();
            
            // 模型训练 (Model training)
            if (hour >= 0 && hour < 6) {
                trainLocalModels(); // 深夜训练模型
            }
        }
        
        // 3. 周末策略 (Weekend strategy)
        if (isWeekend()) {
            setResourceLimit(1.0);  // CPU 100%
            fullModelTraining();     // 全力训练
            knowledgeBaseOptimization(); // 知识库优化
        }
    }
    
    /**
     * 异步任务队列 (Async Task Queue)
     */
    private final PriorityQueue<AsyncTask> taskQueue = new PriorityQueue<>();
    
    public void submitTask(AsyncTask task) {
        taskQueue.offer(task);
        log.info(I18N.get("scheduler.task.submitted"), task.getId());
    }
    
    private void processPendingTasks() {
        while (!taskQueue.isEmpty() && hasResource()) {
            AsyncTask task = taskQueue.poll();
            executeAsync(task);
        }
    }
}
```

---

## 🤖 AI 服务扩展与愿望单系统

### 1. 可扩展的 AI 服务架构

```java
/**
 * AI 服务注册中心 (AI Service Registry)
 */
@Data
public class AIServiceRegistry {
    
    /**
     * 已注册的服务 (Registered services)
     */
    private Map<String, AIService> services = new ConcurrentHashMap<>();
    
    /**
     * 注册新服务 (Register new service)
     */
    public void register(AIService service) {
        services.put(service.getId(), service);
        log.info(I18N.get("service.registered"), service.getName());
    }
    
    /**
     * 预定义服务 (Predefined services)
     */
    public void initializeDefaultServices() {
        // 1. 问答服务 (Q&A Service) - 已实现
        register(new QAService());
        
        // 2. PPT 生成服务 (PPT Generation) - 待实现
        register(new PPTGenerationService());
        
        // 3. 报告生成服务 (Report Generation) - 待实现
        register(new ReportGenerationService());
        
        // 4. 代码生成服务 (Code Generation) - 待实现
        register(new CodeGenerationService());
        
        // 5. 图像理解服务 (Image Understanding) - 已实现
        register(new ImageUnderstandingService());
        
        // 6. 文档总结服务 (Document Summary) - 待实现
        register(new DocumentSummaryService());
        
        // 7. 翻译服务 (Translation) - 待实现
        register(new TranslationService());
        
        // 8. 思维导图生成 (Mind Map) - 待实现
        register(new MindMapService());
    }
}

/**
 * AI 服务接口 (AI Service Interface)
 */
public interface AIService {
    String getId();
    String getName();
    String getDescription();
    ServiceStatus getStatus();
    
    /**
     * 执行服务 (Execute service)
     */
    <T> T execute(ServiceRequest request);
    
    /**
     * 估算成本 (Estimate cost)
     */
    Cost estimateCost(ServiceRequest request);
}
```

### 2. 愿望单系统

```java
/**
 * 愿望单管理器 (Wish List Manager)
 */
@Data
public class WishListManager {
    
    /**
     * 愿望单条目 (Wish list items)
     */
    @Data
    public static class WishItem {
        private String id;
        private String title;           // 愿望标题 (Wish title)
        private String description;     // 详细描述 (Description)
        private String category;        // 分类 (Category)
        private int voteCount;          // 投票数 (Vote count)
        private WishStatus status;      // 状态 (Status)
        private LocalDateTime createdAt;
        private String createdBy;
        private List<String> voters;   // 投票者列表 (Voters)
        private int priority;          // 优先级 (Priority)
        
        /**
         * 计算优先级 (Calculate priority)
         * 基于投票数、创建时间、实现难度
         */
        public int calculatePriority() {
            int votePriority = voteCount * 10;
            int timePriority = (int) ChronoUnit.DAYS.between(createdAt, LocalDateTime.now());
            return votePriority + timePriority;
        }
    }
    
    public enum WishStatus {
        PENDING,      // 等待投票 (Pending votes)
        PLANNED,      // 计划实现 (Planned)
        IN_PROGRESS,  // 开发中 (In progress)
        COMPLETED,    // 已完成 (Completed)
        REJECTED      // 已拒绝 (Rejected)
    }
    
    /**
     * 愿望单存储 (Wish list storage)
     */
    private List<WishItem> wishList = new ArrayList<>();
    
    /**
     * 提交愿望 (Submit wish)
     */
    public WishItem submitWish(String title, String description, String userId) {
        WishItem wish = new WishItem();
        wish.setId(UUID.randomUUID().toString());
        wish.setTitle(title);
        wish.setDescription(description);
        wish.setStatus(WishStatus.PENDING);
        wish.setCreatedAt(LocalDateTime.now());
        wish.setCreatedBy(userId);
        wish.setVoters(new ArrayList<>());
        
        wishList.add(wish);
        log.info(I18N.get("wish.submitted"), title, userId);
        
        return wish;
    }
    
    /**
     * 投票 (Vote)
     */
    public void vote(String wishId, String userId) {
        WishItem wish = findWish(wishId);
        if (wish != null && !wish.getVoters().contains(userId)) {
            wish.getVoters().add(userId);
            wish.setVoteCount(wish.getVoteCount() + 1);
            wish.setPriority(wish.calculatePriority());
            
            log.info(I18N.get("wish.voted"), wishId, userId, wish.getVoteCount());
            
            // 投票数达到阈值，自动转为计划状态 (Auto plan if threshold reached)
            if (wish.getVoteCount() >= 10 && wish.getStatus() == WishStatus.PENDING) {
                wish.setStatus(WishStatus.PLANNED);
                notifyTeam(wish);
            }
        }
    }
    
    /**
     * 获取排行榜 (Get leaderboard)
     */
    public List<WishItem> getLeaderboard() {
        return wishList.stream()
                .filter(w -> w.getStatus() == WishStatus.PENDING || w.getStatus() == WishStatus.PLANNED)
                .sorted((a, b) -> Integer.compare(b.getPriority(), a.getPriority()))
                .collect(Collectors.toList());
    }
    
    /**
     * 按分类统计 (Statistics by category)
     */
    public Map<String, Long> getStatsByCategory() {
        return wishList.stream()
                .collect(Collectors.groupingBy(
                        WishItem::getCategory,
                        Collectors.counting()
                ));
    }
}
```

### 3. 愿望单 UI 设计

```jsx
/**
 * 愿望单组件 (Wish List Component)
 */
const WishListView = () => {
  const [wishes, setWishes] = useState([]);
  const [showAddForm, setShowAddForm] = useState(false);
  
  return (
    <div className="wish-list-container">
      {/* 标题和添加按钮 (Title and Add Button) */}
      <div className="header">
        <h2>🌟 AI 服务愿望单</h2>
        <button onClick={() => setShowAddForm(true)}>
          ➕ 提交新愿望
        </button>
      </div>
      
      {/* 排行榜 (Leaderboard) */}
      <div className="leaderboard">
        <h3>🏆 投票排行榜</h3>
        {wishes.map((wish, index) => (
          <WishCard 
            key={wish.id}
            rank={index + 1}
            wish={wish}
            onVote={handleVote}
          />
        ))}
      </div>
      
      {/* 分类统计 (Category Stats) */}
      <div className="category-stats">
        <h3>📊 分类统计</h3>
        <CategoryChart data={categoryStats} />
      </div>
      
      {/* 添加愿望表单 (Add Wish Form) */}
      {showAddForm && (
        <AddWishForm 
          onSubmit={handleSubmit}
          onCancel={() => setShowAddForm(false)}
        />
      )}
    </div>
  );
};

/**
 * 愿望卡片 (Wish Card)
 */
const WishCard = ({ rank, wish, onVote }) => {
  const hasVoted = wish.voters.includes(currentUserId);
  
  return (
    <div className="wish-card">
      {/* 排名徽章 (Rank Badge) */}
      <div className={`rank-badge rank-${rank}`}>
        #{rank}
      </div>
      
      {/* 愿望内容 (Wish Content) */}
      <div className="wish-content">
        <h4>{wish.title}</h4>
        <p>{wish.description}</p>
        <span className="category-tag">{wish.category}</span>
      </div>
      
      {/* 投票区域 (Vote Area) */}
      <div className="vote-area">
        <button 
          className={`vote-btn ${hasVoted ? 'voted' : ''}`}
          onClick={() => !hasVoted && onVote(wish.id)}
          disabled={hasVoted}
        >
          👍 {wish.voteCount}
        </button>
        
        {hasVoted && <span className="voted-label">已投票</span>}
      </div>
      
      {/* 状态徽章 (Status Badge) */}
      <div className={`status-badge status-${wish.status}`}>
        {getStatusText(wish.status)}
      </div>
    </div>
  );
};
```

---

## 📊 业务数据利用流程

### 流程图

```
用户使用 AI 服务
    ↓
生成内容（PPT、报告、代码等）
    ↓
用户授权数据收集 ← 关键！
    ↓
┌─────────────────────────────┐
│   数据收集与存储              │
│   (Data Collection)          │
│                              │
│   - 原始输入                  │
│   - 生成结果                  │
│   - 用户修改（重要！）        │
│   - 反馈评分                  │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│   自动标注与提取              │
│   (Auto Annotation)          │
│                              │
│   - 提取业务术语              │
│   - 识别概念关系              │
│   - 构建知识图谱              │
└─────────────────────────────┘
    ↓
┌─────────────────────────────┐
│   模型微调（下班时间）        │
│   (Model Fine-tuning)        │
│                              │
│   - 周末批量训练              │
│   - 持续优化准确率            │
│   - A/B 测试验证              │
└─────────────────────────────┘
    ↓
本地模型越来越准确！
    ↓
用户体验越来越好！
    ↓
更多人使用 → 更多数据 → 更好模型
    ↓
正向循环！🔄
```

---

## 🎯 实施方案

### Phase 4.6.1: 智能路由与本地模型集成 (3-4天)

```yaml
任务:
  - [ ] AI 模型智能路由器
  - [ ] 本地模型引擎（Qwen2.5-7B）
  - [ ] 云端模型引擎（GPT-5 API）
  - [ ] 混合模式实现

文件:
  - src/main/java/top/yumbo/ai/rag/ai/AIModelRouter.java (~300行)
  - src/main/java/top/yumbo/ai/rag/ai/LocalModelEngine.java (~350行)
  - src/main/java/top/yumbo/ai/rag/ai/CloudModelEngine.java (~250行)
  - src/main/java/top/yumbo/ai/rag/ai/HybridExecutor.java (~280行)

技术选型:
  本地模型:
    - Qwen2.5-7B-Instruct (推荐)
    - Llama3-8B-Instruct
    - Phi-3-mini-4k
  
  推理框架:
    - llama.cpp (CPU 推理)
    - vLLM (GPU 推理)
    - Ollama (本地部署)

验证:
  - 路由准确率: > 90%
  - 本地模型响应: < 2秒
  - 云端模型响应: < 5秒
  - 成本降低: 70%+
```

### Phase 4.6.2: 资源调度系统 (2-3天)

```yaml
任务:
  - [ ] 智能资源调度器
  - [ ] 异步任务队列
  - [ ] 时间策略管理
  - [ ] 系统负载监控

文件:
  - src/main/java/top/yumbo/ai/rag/scheduler/ResourceScheduler.java (~320行)
  - src/main/java/top/yumbo/ai/rag/scheduler/AsyncTaskQueue.java (~250行)
  - src/main/java/top/yumbo/ai/rag/scheduler/TimeStrategy.java (~200行)
  - src/main/java/top/yumbo/ai/rag/scheduler/SystemMonitor.java (~180行)

验证:
  - 工作时间 CPU 占用: < 20%
  - 下班时间 CPU 占用: 60-80%
  - 任务调度延迟: < 1秒
  - 系统稳定性: 99.9%
```

### Phase 4.6.3: 业务数据收集与利用 (3-4天)

```yaml
任务:
  - [ ] 数据收集器（用户授权）
  - [ ] 自动标注系统
  - [ ] 概念提取器
  - [ ] 知识图谱构建

文件:
  - src/main/java/top/yumbo/ai/rag/data/BusinessDataCollector.java (~300行)
  - src/main/java/top/yumbo/ai/rag/data/AutoAnnotator.java (~350行)
  - src/main/java/top/yumbo/ai/rag/data/ConceptExtractor.java (~280行)
  - src/main/java/top/yumbo/ai/rag/data/KnowledgeGraphBuilder.java (~320行)

验证:
  - 数据收集合规性: 100%
  - 概念提取准确率: > 80%
  - 标注效率: > 1000条/小时
```

### Phase 4.6.4: AI 服务扩展框架 (2-3天)

```yaml
任务:
  - [ ] AI 服务注册中心
  - [ ] 服务插件化架构
  - [ ] PPT 生成服务（示例）
  - [ ] 服务监控与统计

文件:
  - src/main/java/top/yumbo/ai/rag/service/AIServiceRegistry.java (~250行)
  - src/main/java/top/yumbo/ai/rag/service/AIService.java (~150行)
  - src/main/java/top/yumbo/ai/rag/service/PPTGenerationService.java (~400行)
  - src/main/java/top/yumbo/ai/rag/service/ServiceMonitor.java (~200行)

验证:
  - 服务注册成功率: 100%
  - PPT 生成质量: > 4.0/5.0
  - 服务可用性: > 99%
```

### Phase 4.6.5: 愿望单系统 (2天)

```yaml
任务:
  - [ ] 愿望单管理器（后端）
  - [ ] 愿望单 UI（前端）
  - [ ] 投票排行榜
  - [ ] 自动通知系统

文件:
  后端:
    - src/main/java/top/yumbo/ai/rag/wish/WishListManager.java (~300行)
    - src/main/java/top/yumbo/ai/rag/wish/WishItem.java (~150行)
    - src/main/java/top/yumbo/ai/rag/wish/VotingSystem.java (~200行)
  
  前端:
    - src/main/resources/static/js/components/WishListView.jsx (~350行)
    - src/main/resources/static/css/wish-list.css (~150行)

验证:
  - 投票实时性: < 1秒
  - 排行榜更新: 实时
  - UI 响应速度: < 500ms
```

---

## 📈 预期效果

### 技术效果

```yaml
成本降低:
  - API 调用成本: ↓ 70%
  - 服务器成本: 利用闲置资源，↓ 0元

性能提升:
  - 简单任务响应: < 2秒（本地）
  - 复杂任务响应: < 5秒（云端）
  - 系统可用性: 99.9%

准确率提升:
  - 初期: 本地模型 80%，云端 95%
  - 3个月后: 本地模型 90%（业务数据微调）
  - 6个月后: 本地模型 95%（超越云端）
```

### 业务效果

```yaml
用户满意度:
  - 响应速度更快: 用户满意度 ↑ 30%
  - 理解更精准: 业务准确率 ↑ 40%
  - 成本更低: 企业成本 ↓ 70%

知识积累:
  - 业务数据: 每月 + 10,000 条
  - 概念单元: 每月 + 1,000 个
  - 知识图谱: 持续完善

服务扩展:
  - 初期服务: 8 个
  - 6 个月后: 20+ 个（根据愿望单）
  - 用户活跃度: ↑ 200%
```

---

## 🎯 关键成功因素

### 1. 用户授权与隐私保护

```yaml
必须做到:
  ✅ 明确告知用户数据用途
  ✅ 用户主动授权（opt-in）
  ✅ 可随时撤销授权
  ✅ 敏感数据脱敏处理
  ✅ 数据安全加密存储

用户界面:
  "💡 提示：您可以选择贡献数据帮助改进系统
   - ✅ 提升 AI 准确率
   - ✅ 加快响应速度
   - ✅ 获得积分奖励
   
   [ ] 我同意贡献匿名化的使用数据
   [ ] 我同意贡献内容数据（可随时撤销）"
```

### 2. 渐进式实施

```yaml
阶段 1 (当前):
  - 实施智能路由
  - 集成本地模型
  - 验证效果

阶段 2 (3个月):
  - 收集业务数据
  - 首次模型微调
  - 对比效果

阶段 3 (6个月):
  - 持续优化
  - 本地模型为主
  - 云端为辅
```

### 3. 愿望单驱动开发

```yaml
流程:
  1. 用户提交愿望
  2. 社区投票排序
  3. 每月实现 Top 3
  4. 持续扩展服务

优势:
  - 用户参与感强
  - 开发方向明确
  - 资源投入高效
```

---

## 🎉 总结

这个方案完美解决了您的所有担忧：

### ✅ 问题 1: 概念单元精准性
- **解决**: 本地模型 + 业务数据微调 → 准确率持续提升

### ✅ 问题 2: 本地资源优化
- **解决**: 智能调度 + 异步处理 → 工作时间占用 < 20%

### ✅ 问题 3: 业务数据价值
- **解决**: 数据收集 + 自动标注 → 训练业务专属模型

### ✅ 问题 4: AI 服务扩展
- **解决**: 插件化架构 + PPT 等服务 → 持续扩展

### ✅ 问题 5: 愿望单机制
- **解决**: 投票排行 + 自动通知 → 用户驱动开发

---

**预期代码量**: ~3,000行  
**预计工作量**: 2-3周  
**关键技术**: Qwen2.5-7B + llama.cpp + 异步调度  
**核心价值**: 成本 ↓ 70%，准确率 ↑ 40%，用户满意度 ↑ 30%

---

**下一步**: 请决策是否实施 Phase 4.6？

