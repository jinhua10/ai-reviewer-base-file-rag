# Markdown 完整支持升级指南

## 🎯 升级内容

将简单的 Markdown 解析器升级为功能完整的 **react-markdown** 库，支持：

### ✅ 新增支持的格式

| 格式 | 语法 | 说明 |
|------|------|------|
| **标题** | `# H1` ~ `###### H6` | 6级标题，带底部边框 |
| **表格** | `\| Col1 \| Col2 \|` | GitHub 风格表格，响应式 |
| **任务列表** | `- [ ] Task` | 可勾选的任务列表 |
| **删除线** | `~~text~~` | 删除线文本 |
| **分隔线** | `---` 或 `***` | 水平分隔线 |
| **引用块** | `> Quote` | 引用样式 |
| **有序列表** | `1. Item` | 数字列表 |
| **无序列表** | `- Item` 或 `* Item` | 项目符号列表 |
| **嵌套列表** | 缩进 + `- Item` | 多级嵌套 |
| **图片** | `![alt](url)` | 图片显示（已优化） |
| **链接** | `[text](url)` | 超链接 |
| **代码块** | ` ```lang ` | 语法高亮代码块 |
| **行内代码** | `` `code` `` | 行内代码 |
| **粗体** | `**text**` | 加粗文本 |
| **斜体** | `*text*` | 斜体文本 |

---

## 📦 安装依赖

### Windows (PowerShell)

```powershell
cd UI
npm install react-markdown@9.0.1 remark-gfm@4.0.0 rehype-raw@7.0.0
```

### Linux/Mac

```bash
cd UI
npm install react-markdown@9.0.1 remark-gfm@4.0.0 rehype-raw@7.0.0
```

---

## 🔧 已修改的文件

### 1. `UI/package.json`
添加了三个新依赖：
```json
"react-markdown": "^9.0.1",    // 核心 Markdown 渲染库
"remark-gfm": "^4.0.0",        // GitHub Flavored Markdown 扩展
"rehype-raw": "^7.0.0"         // 支持原始 HTML
```

### 2. `UI/src/components/qa/MarkdownRenderer.jsx`
完全重写，使用 `react-markdown` 替代自定义解析器：
- ✅ 自动解析所有 Markdown 语法
- ✅ 自定义组件渲染（代码块、图片、表格等）
- ✅ 保留原有的 CodeBlock 高亮功能
- ✅ 添加安全属性（链接打开新窗口）

### 3. `UI/src/assets/css/qa/markdown-renderer.css`
新增完整的样式支持：
- ✅ 标题样式（6级，带底部边框）
- ✅ 表格样式（渐变表头、悬停效果、响应式）
- ✅ 列表样式（嵌套支持、任务列表）
- ✅ 引用块样式
- ✅ 分隔线样式
- ✅ 删除线样式

---

## 🧪 测试示例

### Markdown 测试内容

```markdown
# 一级标题
## 二级标题
### 三级标题

**粗体文本** *斜体文本* ~~删除线~~ `行内代码`

[链接示例](https://example.com)

![图片示例](https://via.placeholder.com/300)

---

### 无序列表
- 项目 1
- 项目 2
  - 子项目 2.1
  - 子项目 2.2
- 项目 3

### 有序列表
1. 第一项
2. 第二项
3. 第三项

### 任务列表
- [x] 已完成任务
- [ ] 待办任务
- [ ] 另一个待办

### 表格示例

| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据1 | 数据2 | 数据3 |
| A | B | C |

### 引用
> 这是一段引用文本
> 可以跨越多行

### 代码块
\`\`\`javascript
function hello() {
  console.log("Hello World!");
}
\`\`\`
```

---

## 🎨 样式效果

### 表格
- 渐变紫色表头
- 悬停高亮行
- 响应式滚动（窄屏）
- 圆角阴影

### 标题
- H1/H2 带底部边框
- 渐进式字体大小
- 合理的间距

### 图片
- 圆角阴影
- 悬停放大效果
- 懒加载
- 响应式宽度

---

## 🚀 启动验证

### 1. 安装依赖
```powershell
cd UI
npm install
```

### 2. 启动开发服务器
```powershell
npm run dev
```

### 3. 测试 Markdown
在问答界面输入包含表格、列表、代码块的问题，查看渲染效果。

---

## 📊 性能对比

| 特性 | 旧版本 | 新版本 |
|------|--------|--------|
| 代码块 | ✅ | ✅ |
| 粗体/斜体 | ✅ | ✅ |
| 链接 | ✅ | ✅ |
| 图片 | ❌ → ✅ | ✅ |
| 标题 | ❌ | ✅ |
| 表格 | ❌ | ✅ |
| 列表 | ✅ (基础) | ✅ (完整) |
| 任务列表 | ❌ | ✅ |
| 删除线 | ❌ | ✅ |
| 引用块 | ✅ (基础) | ✅ (优化) |
| 分隔线 | ❌ | ✅ |
| 嵌套列表 | ❌ | ✅ |

---

## ⚠️ 注意事项

1. **Node.js 版本**：需要 Node.js >= 16
2. **依赖冲突**：如果安装失败，先删除 `node_modules` 和 `package-lock.json`
3. **兼容性**：`react-markdown` 9.x 需要 React 18+（已满足）

---

## 🐛 故障排除

### 问题：npm install 失败
```powershell
# 清除缓存
npm cache clean --force
# 删除旧依赖
Remove-Item -Recurse -Force node_modules, package-lock.json
# 重新安装
npm install
```

### 问题：表格不显示
检查浏览器控制台是否有 CSS 加载错误。

### 问题：代码高亮失效
确保 `CodeBlock` 组件正常工作，`highlight.js` 已安装。

---

## 📚 参考文档

- [react-markdown](https://github.com/remarkjs/react-markdown)
- [remark-gfm](https://github.com/remarkjs/remark-gfm)
- [GitHub Flavored Markdown](https://github.github.com/gfm/)
